import React,{useEffect,useMemo,useState} from 'react';import{createRoot}from'react-dom/client';import'./styles.css';

const CHIP_PRESETS=[1000,2500,5000,10000];const BLIND_PRESETS=[5,10,15,20,30];
function App(){const [view,setView]=useState('home');const [name,setName]=useState(localStorage.getItem('ftp_name')||'');const [code,setCode]=useState('');const [chips,setChips]=useState(2500);const [blindMins,setBlindMins]=useState(10);const [customChips,setCustomChips]=useState('');const [customBlind,setCustomBlind]=useState('');const [table,setTable]=useState(null);const [msg,setMsg]=useState('');
const effectiveChips=customChips?Math.max(100,Number(customChips)):chips;const effectiveBlind=customBlind?Math.max(1,Number(customBlind)):blindMins;
async function createTable(){if(!name.trim())return setMsg('Enter a display name.');localStorage.setItem('ftp_name',name.trim());const r=await fetch('/api/tables',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({hostName:name.trim(),startingChips:effectiveChips,blindMinutes:effectiveBlind})});const j=await r.json();if(!r.ok)return setMsg(j.error||'Could not create table.');location.hash=`#/table/${j.code}?token=${j.token}`}
async function joinTable(){if(!name.trim()||!code.trim())return setMsg('Enter your name and game code.');localStorage.setItem('ftp_name',name.trim());const r=await fetch(`/api/tables/${code.trim().toUpperCase()}/join`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name:name.trim()})});const j=await r.json();if(!r.ok)return setMsg(j.error||'Could not join table.');location.hash=`#/table/${j.code}?token=${j.token}`}
useEffect(()=>{const route=()=>{const m=location.hash.match(/^#\/table\/([A-Z0-9]+)/i);if(m){setView('table');setCode(m[1].toUpperCase())}else setView('home')};route();addEventListener('hashchange',route);return()=>removeEventListener('hashchange',route)},[]);
if(view==='table')return <Table code={code} onExit={()=>{location.hash='';setView('home')}}/>;
return <main className="shell"><header className="brand"><div className="mark">FT</div><div><h1>FULL TILT POKER</h1><p>Private tournament poker. Last stack standing.</p></div></header><section className="hero"><div><span className="eyebrow">PRIVATE HOLD’EM</span><h2>Your table. Your people.<br/>No casino bullshit.</h2><p>Create a private tournament, send the code, and play until one player owns every chip on the table.</p></div><div className="cards"><span>♠</span><span>♥</span><span>♦</span><span>♣</span></div></section><section className="grid"><div className="panel"><h3>Create Tournament</h3><label>Your display name<input value={name} onChange={e=>setName(e.target.value)} placeholder="Sam" maxLength={24}/></label><label>Starting chips</label><div className="choices">{CHIP_PRESETS.map(v=><button className={!customChips&&chips===v?'active':''} onClick={()=>{setChips(v);setCustomChips('')}}>{v.toLocaleString()}</button>)}</div><input value={customChips} onChange={e=>setCustomChips(e.target.value.replace(/\D/g,''))} placeholder="Custom chips" inputMode="numeric"/><label>Blind level timer</label><div className="choices">{BLIND_PRESETS.map(v=><button className={!customBlind&&blindMins===v?'active':''} onClick={()=>{setBlindMins(v);setCustomBlind('')}}>{v} min</button>)}</div><input value={customBlind} onChange={e=>setCustomBlind(e.target.value.replace(/\D/g,''))} placeholder="Custom minutes" inputMode="numeric"/><button className="primary" onClick={createTable}>Create Private Table</button></div><div className="panel"><h3>Join Tournament</h3><label>Your display name<input value={name} onChange={e=>setName(e.target.value)} placeholder="Sam" maxLength={24}/></label><label>Game code<input value={code} onChange={e=>setCode(e.target.value.toUpperCase().replace(/[^A-Z0-9]/g,''))} placeholder="FT7K2Q" maxLength={6}/></label><button className="primary" onClick={joinTable}>Take a Seat</button><div className="rules"><b>v0.1 rules</b><span>2–9 players</span><span>No rebuys after the game starts</span><span>0 chips = eliminated</span><span>Host controls start/pause/end</span><span>Blind timer lives on the server</span></div></div></section>{msg&&<div className="toast" onClick={()=>setMsg('')}>{msg}</div>}<footer>FULL TILT • PRIVATE PLAY CHIP TOURNAMENTS</footer></main>}

function Table({code,onExit}){const params=new URLSearchParams(location.hash.split('?')[1]||'');const token=params.get('token')||'';const [state,setState]=useState(null);const [err,setErr]=useState('');const [raise,setRaise]=useState('');async function load(){const r=await fetch(`/api/tables/${code}?token=${encodeURIComponent(token)}`);const j=await r.json();if(r.ok)setState(j);else setErr(j.error||'Table not found.')}useEffect(()=>{load();const id=setInterval(load,1500);return()=>clearInterval(id)},[code,token]);async function action(type,amount){const r=await fetch(`/api/tables/${code}/action`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({token,type,amount:Number(amount)||0})});const j=await r.json();if(!r.ok)setErr(j.error||'Action failed.');else setState(j)}if(!state)return <main className="tablePage"><button className="ghost top" onClick={onExit}>← Lobby</button><div className="loading">{err||'Pulling up a chair…'}</div></main>;const me=state.players.find(p=>p.token===token);return <main className="tablePage"><div className="topbar"><button className="ghost" onClick={onExit}>← Lobby</button><div><b>TABLE {code}</b><span> Blinds {state.smallBlind}/{state.bigBlind} • Level {state.blindLevel+1} • {state.blindMinutes} min</span></div><button className="ghost" onClick={()=>navigator.clipboard?.writeText(location.href.split('?')[0])}>Copy table link</button></div><section className="felt"><div className="board">{state.board.map((c,i)=><Card key={i} c={c}/>)}{state.board.length===0&&<span className="waiting">{state.started?'Pre-flop':'Waiting for host to start'}</span>}</div><div className="pot">POT {state.pot.toLocaleString()}</div>{state.players.map((p,i)=><div className={`seat seat${i} ${p.turn?'turn':''} ${p.folded?'folded':''}`} key={p.id}><div className="avatar">{p.name.slice(0,2).toUpperCase()}</div><b>{p.name}{p.host?' ★':''}</b><span>{p.chips.toLocaleString()}</span>{p.bet>0&&<small>Bet {p.bet}</small>}{p.eliminated&&<em>OUT</em>}</div>)}</section><section className="hud"><div className="hole">{me?.cards?.map((c,i)=><Card key={i} c={c}/>)||<span>Cards hidden</span>}</div><div className="status">{state.message}</div><div className="actions">{me?.host&&!state.started&&<button className="primary" onClick={()=>action('start')}>Start Tournament</button>}{state.started&&me?.turn&&!me?.folded&&!me?.eliminated&&<><button onClick={()=>action('fold')}>Fold</button><button onClick={()=>action(state.toCall===0?'check':'call')}>{state.toCall===0?'Check':`Call ${state.toCall}`}</button><input value={raise} onChange={e=>setRaise(e.target.value.replace(/\D/g,''))} placeholder={`Raise ≥ ${state.minRaise}`} inputMode="numeric"/><button className="primary" onClick={()=>action('raise',raise)}>Raise</button></>}{me?.host&&state.started&&<button className="danger" onClick={()=>action('end')}>End Game</button>}</div></section>{err&&<div className="toast" onClick={()=>setErr('')}>{err}</div>}</main>}
function Card({c}){if(!c)return <div className="card back">FT</div>;const red=c.endsWith('♥')||c.endsWith('♦');return <div className={`card ${red?'red':''}`}><b>{c.slice(0,-1)}</b><span>{c.slice(-1)}</span></div>}
createRoot(document.getElementById('root')).render(<App/>);